<h2>Recently added</h2>
<div>
  <% books.forEach(book=>{%>
    <% if(book.coverImagePath) {%>
    <img width="100px" height="150px" src="<%= book.coverImagePath %>" alt="">
    <% } %>
    <% }) %>
</div>
